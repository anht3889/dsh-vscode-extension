# Dependencies
node_modules/

# Build output
dist/
out/
lib/

# pnpm / npm store + caches
.pnpm-store/
.npm-cache/
*.tsbuildinfo

# Task 0 spike install byproducts (Task 1 owns the real root workspace + lockfile)
packages/bridge/pnpm-lock.yaml
packages/bridge/pnpm-workspace.yaml
